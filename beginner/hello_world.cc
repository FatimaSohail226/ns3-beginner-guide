#include "ns3/core-module.h"  // Include the core module of ns-3

using namespace ns3;  // Import the ns3 namespace into the current scope

int main()
{
    // Initialize the ns-3 system

    // Print "Hello World!" to the console using the ns-3 logging system
    // NS_LOG_UNCOND is a macro for logging messages
    NS_LOG_UNCOND("Hello World!");

    // Exit the program with a return value of 0 (indicating success)
    return 0;
}
